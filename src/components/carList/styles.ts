import styled from 'styled-components';

const Layout = styled.section`
  height: 100%;
  display: flex;
  align-items: center;
  justify-content: center;
  overflow-y: auto;
  h3 {
    font-weight: bold;
    font-size: 1.2rem;
  }
`;

const S = {
  Layout,
};

export default S;
